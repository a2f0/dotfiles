# Use a login shell
set -g default-command "${SHELL}"

setw -g mode-keys vi

set-option -g status-position top

# cntrl + shift <left arrow> / <right arrow> - move window left / right
bind-key -n C-S-Left swap-window -t -1
bind-key -n C-S-Right swap-window -t +1
