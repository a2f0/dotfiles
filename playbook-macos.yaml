---
- hosts: 127.0.0.1
  connection: local
  become: no
  vars:
    dotfiles_dir: "{{ playbook_dir }}"
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - include: tasks/macos/install-homebrew.yaml
    - include: tasks/clone-anduril-into-home.yaml
    - include: tasks/macos/install-homebrew-packages.yaml
    - include: tasks/files.yaml
      tags:
        - files
    - include: tasks/macos/files.yaml
      tags:
        - files
